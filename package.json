{
  "name": "west-elm",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.19.0",
    "cors": "^2.8.5",
    "cypress": "^3.3.1",
    "express": "^4.17.1",
    "lodash.get": "^4.4.2",
    "node-fetch": "^2.6.1",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "3.0.1"
  },
  "scripts": {
    "start": "lsof -ti:3000 | xargs kill && npm run start-server && react-scripts start",
    "start-server": "lsof -ti:3001 | xargs kill && node server.js&",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "cypress": "cypress open",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "eslint-plugin-cypress": "^2.2.1"
  }
}
